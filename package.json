{
  "name": "codegrapher",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "lint:specs": "node scripts/lint-specs.mjs",
    "lint:trace-css": "node scripts/guard-trace-css.mjs",
    "lint:trace-motion-sync": "node scripts/lint-trace-motion-sync.mjs",
    "design:check": "npm run lint:tokens && npm run lint:trace-css && npm run lint:trace-motion-sync && npm test -- --run traceMachine wireReveal traceDepth",
    "lint:tokens": "node scripts/lint-tokens.mjs",
    "lint:file-length": "node scripts/lint-file-length.mjs",
    "test": "npm run test --prefix client",
    "stress:load": "node scripts/stress-load-flow.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
