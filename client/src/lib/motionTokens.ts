/** Keep in sync with `--motion-trace` / `--motion-trace-out` / `--motion-trace-pending` in `styles/theme-light.css`. */
export const MOTION_TRACE_MS = 120;
export const MOTION_TRACE_OUT_MS = 80;
export const MOTION_TRACE_PENDING_MS = 80;
/** Keep in sync with `--ease-trace-out`. */
export const EASE_TRACE_OUT = "cubic-bezier(0.4, 0, 1, 1)";
